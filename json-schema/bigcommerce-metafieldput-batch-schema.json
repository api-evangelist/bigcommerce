{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/MetafieldPut_Batch",
  "title": "MetafieldPut_Batch",
  "description": "The model for a PUT to create metafield.\n",
  "allOf": [
    {
      "$ref": "#/components/schemas/MetafieldBase_Post"
    },
    {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "example": 42,
          "description": "The ID of metafield to update.\n"
        },
        "resource_id": {
          "type": "integer",
          "example": 42,
          "description": "The ID for the resource with which the metafield is associated.\n"
        }
      },
      "required": [
        "id"
      ]
    }
  ],
  "x-internal": false
}